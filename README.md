# statwolf-new-component-plugin package

Atom plugin for creating new Statwolf components.

The package uses `statwolf-components` npm package, which is private, but any template engine could do the trick.
